sudo: false
language: java

jdk:
  - oraclejdk8

cache:
  directories:
    - "$HOME/.m2/repository"
    - "$HOME/.m2/wrapper"

install: true

jobs:
  include:
    - stage: test
      script: ./mvnw --show-version --batch-mode --errors test
